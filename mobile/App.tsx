/**
 * Root component. Wraps the tree in providers:
 *   QueryClientProvider -> AuthProvider -> I18nProvider -> ThemeProvider
 *   -> NavigationContainer(RootNavigator)
 * plus offline sync bootstrap.
 */
export default function App() {
  return null; // TODO
}
