# shared/

Small, dependency-free modules imported by **both** `backend/` and `mobile/`:
role/enum names, permission-matrix helpers, shared DTO shapes. Keep this
framework-agnostic (plain JS/TS, no Express or React imports).
