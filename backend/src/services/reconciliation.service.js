/**
 * Phase 3: cross-check MEO physical_progress_pct against financial_year_allocations.financial_progress_pct, surface mismatches.
 * Business logic only — no HTTP objects, no raw SQL. Calls repositories/ for
 * data and other services/ for cross-cutting actions. Unit-testable in isolation.
 */
'use strict';
module.exports = {};
