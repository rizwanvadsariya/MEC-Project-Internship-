# ADP source data

Drop the official ADP booklet ledger export(s) here (JSON/CSV) for
`../importAdpBooklet.js` to read. These raw files are git-ignored by default —
commit a small sample only if the team agrees.
