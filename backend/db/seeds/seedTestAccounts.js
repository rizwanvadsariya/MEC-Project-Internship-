/** Creates one Supabase Auth user per role (RD, DG, MEO, SUPPORT_USER) with a
 *  division/department assigned, and the mirror rows in the app `users` table.
 *  phases.md Step 3. Dev/staging only. */
'use strict';
