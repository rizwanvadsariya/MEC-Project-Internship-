/** Unit: approve/reject writes an append-only request row and flips team status;
 *  APPROVE creates exactly one site_visit. */
'use strict';

test.todo('approve writes an append-only team_approval_requests row and flips visit_teams.status to APPROVED');
test.todo('approve creates exactly one site_visit for the team');
test.todo('reject writes an append-only team_approval_requests row and flips visit_teams.status to REJECTED');

