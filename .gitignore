# ---- Node ----
node_modules/
npm-debug.log*
yarn-error.log*
.pnp.*

# ---- Env / secrets ----
.env
.env.*
!.env.example

# ---- Builds / caches ----
dist/
build/
coverage/
.expo/
.expo-shared/
*.tsbuildinfo

# ---- Mobile native ----
mobile/android/
mobile/ios/
*.keystore
*.p8
*.mobileprovision

# Raw source photos for the login screen background — the app reads the
# copies in mobile/assets/images/login/ instead; this is just the original
# drop folder, not needed in the repo.
mobile/MEC pictures/

# ---- OS / editor ----
.DS_Store
Thumbs.db
.idea/
.vscode/
